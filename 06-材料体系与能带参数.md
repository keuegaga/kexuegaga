# 材料体系与能带参数

> 目标：QCL 选材料时知道每个体系的带阶、应变、温度行为，以及如何在 nextnano 里校准带阶。

## 1. 主流材料体系

### 1.1 GaAs / AlₓGa₁₋ₓAs（最经典）

- 覆盖中红外（~8–12 µm）与太赫兹（~2–5 THz）
- 晶格匹配，无应变问题，MBE 工艺成熟、成本低
- 导带带阶随 x 增大：x=0.15 时约 152 meV（Andrews THz），x=0.45 时约 390 meV（Page 中红外）
- LO 声子能量约 36 meV → 天然适合谐振声子去填充（THz）
- 代表性文献：Page2001（9 µm）、Sirtori1998（9.4 µm）、Rochat2002（66 µm）、Hu2005（89.2 µm）、Andrews2008（107 µm）

### 1.2 In₀.₅₃Ga₀.₄₇As / Al₀.₄₈In₀.₅₂As（InP 衬底）

- 晶格匹配于 InP，**带阶更大（CBO ≈ 0.52 eV）**，适合更短波长中红外（~3–10 µm）与更高温度工作
- 可以靠**应变**进一步调带阶/能带（应变补偿超晶格）
- 代表文献：Friedrich2005（10 µm，无注入区设计）、Bai2010（275 meV ≈ 4.5 µm，NEGF 增益优化教程用的体系）
- nextnano 中阱写法：`ternary_constant{ name = "In(x)Ga(1-x)As" alloy_x = 0.53 }`，垒：`"Al(x)In(1-x)As" alloy_x = 0.48`

### 1.3 Si/SiGe（IV 族）

- 电致发光验证级（Dehlinger2000，Science 290, 2277，λ≈9.9 µm，实验 125 meV）
- 与 CMOS 工艺兼容是卖点，但增益仍难
- nextnano 示例：`1DQuantumCascadeLaserSiGe_nnp.nnp`

### 1.4 其他

- InAs/AlSb 体系：超大带阶，短波中红外（λ<6 µm）
- GaN 基：蓝宝石/自支撑 GaN 衬底，极化电场强，QCSE 影响大（进阶课题）

## 2. 应变的作用

QCL 中应变主要用来**改变导带带阶与子带能级**，从而调谐波长：

- 压应变阱（InGaAs 中 In 组分高）：提高带阶、降低有效质量 → 波长可调
- 应变补偿：阱压应变 + 垒张应变交替，维持总应变低于临界厚度
- nextnano 会自动算应变（`crystal_zb` + `substrate` 设置后），并考虑应变导致的能带移动

**提示**：先做无应变体系（GaAs/AlGaAs）练手，再上应变体系（InGaAs/AlInAs）。

## 3. 带阶（CBO）怎么定与怎么校准

### 3.1 为什么必须校准

nextnano 材料库给出的是"默认带阶"，而论文设计常用**自己的带阶值**（来自特定测量或 X 组分模型）。带阶差 10–20 meV 就会让跃迁能量偏几十 meV，直接导致波长对不上。

### 3.2 校准方法

**方法 1：改 bowing（Page2001 的做法）**

```python
database{
    bowing_zb{
        name = "AlGaAs_Bowing_Ga"
        valence = III_V
        valence_bands{ bandoffset = -0.091925 }
    }
    bowing_zb{
        name = "AlGaAs_Bowing_Al"
        valence = III_V
        valence_bands{ bandoffset = -0.091925 }
    }
}
```

**方法 2：直接改三元材料带阶**（nextnano++ 支持在 region 或 database 中覆盖 band offset 相关参数）

**方法 3：THz 参数化模板**里直接声明 `$CBO = 0.1518` 并在文件内使用。

### 3.3 校准的验证方法

跑一个"平带 + 单阱"或直接跑官方示例，检查：

- 无限深阱解析解 En ∝ n² 对照（见 [[07-子带间跃迁-矩阵元-增益]]）
- 官方示例的基准值（Page2001：z₁₀,₆ = 1.6655 nm、ΔE = 147.7 meV）

## 4. 温度的影响

- 温度升高 → 带隙变小、带阶微变、LO 声子占据数上升 → 上态寿命变短
- 中红外 QCL 300 K 工作需仔细设计注入；THz QCL 通常 10–100 K 工作
- 输入文件里 `temperature = 300.0` 是全局参数，材料参数会自动按温度插值
- 对比实验时务必用论文报告的器件温度（77 K / 300 K）

## 5. 单带 vs k.p 模型选择

| 情况 | 推荐模型 |
|---|---|
| GaAs/AlGaAs 中红外，初步设计 | 单带有效质量（快、够准） |
| InGaAs/AlInAs，非抛物性明显 | k.p（如 Friedrich2005 的 `_kp` 文件） |
| 需要价带/带间信息（ICL） | 8 带 k.p |
| 子带能量对有效质量敏感 | 两者都跑一遍对比 |

nextnano 示例里 Friedrich2005 同时提供 `_kp` 和 `_sg`（single group）两个版本，可直接对比。

## 6. 选材速查

```text
目标波长        → 推荐体系
---------------------------------------------------------
3–6 µm 中红外   → InGaAs/AlInAs (InP)，应变可调
7–12 µm 中红外  → GaAs/AlGaAs 或 InGaAs/AlInAs
1.2–5 THz       → GaAs/AlGaAs（低 Al 组分），LO 声子去填充
Si 兼容演示      → Si/SiGe
超短波/大带阶    → InAs/AlSb
```

## 7. 相关笔记

- 带阶直接影响跃迁能量 → [[07-子带间跃迁-矩阵元-增益]]
- 网格与数值细节 → [[08-边界条件-网格-数值技巧]]
- 完整设计流程 → [[09-QCL设计流程Checklist]]
