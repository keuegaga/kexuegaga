# nextnano++ 输入文件语法入门

> 目标：读懂任何一个 `.nnp` 文件，并能从模板出发写自己的 QCL 输入文件。

## 1. 文件总体结构

一个 `.nnp` 文件由若干**块（block）**组成，块内可以嵌套。注释用 `#`。可定义 `$变量` 做参数化。

```
!TEXT ... !ENDTEXT    元信息（标题、文献）
$变量                 参数定义（厚度、电场、温度、掺杂…）
output{ }             控制输出
run{ }                控制计算流程（解什么方程）
global{ }             维度、晶向、衬底、温度
contacts{ }           接触/电极（含偏压）
structure{ }          材料几何（各区域）
grid{ }               网格
classical{ }          经典量（能带边、载流子密度）
poisson{ }            泊松方程设置（电场/自洽）
quantum{ }            薛定谔方程设置（量子区、本征态、输出）
database{ }           覆盖默认材料参数（如带阶校准）
```

## 2. 各块详解（以 QCL 视角）

### 2.1 `global`：全局设置

```python
global{
    simulate1D{ }                          # 1D 仿真（QCL 初步设计用 1D 足够）
    crystal_zb{ x_hkl = [1, 0, 0] y_hkl = [0, 1, 0] }
    substrate{ name = "GaAs" }             # 衬底决定应变参考
    temperature = 300.0
}
```

- 纤锌矿材料（氮化物）用 `crystal_wz`，这里闪锌矿 `crystal_zb`
- 温度影响带隙、带阶等参数，**论文里说"77 K 工作"就设 77**

### 2.2 `structure`：层序几何

QCL = 沿 x 方向（生长方向）交替的阱/垒层。每层一个 `region`：

```python
structure{
    region{                                 # 背景默认材料（写 everywhere 兜底）
        everywhere{ }
        ternary_constant{ name = "Al(x)Ga(1-x)As" alloy_x = 0.45 }
        contact{ name = fermi_zero }        # 把默认区域接上接触
    }
    region{                                 # 4.6 nm 垒
        line{ x = [0, 4.6] }
        ternary_constant{ name = "Al(x)Ga(1-x)As" alloy_x = 0.45 }
    }
    region{                                 # 1.9 nm 阱
        line{ x = [4.6, 6.5] }
        binary{ name = "GaAs" }
    }
    # …… 依此类推，坐标必须连续
}
```

要点：

- 坐标单位 **nm**，层与层坐标首尾相接
- `binary{ name = "GaAs" }` / `ternary_constant{ name = "Al(x)Ga(1-x)As" alloy_x = 0.45 }` 指定材料
- 掺杂层写法：`doping{ constant{ name = "fully-ionized" conc = 2.8e15 } }`，conc 单位 **cm⁻³**
- 周期重复：在 region 内加 `array_x{ max = 3 shift = $LENGTH_OF_PERIOD }`（见 [[05-示例2-Capasso简单级联与THz参数化]]）

### 2.3 `grid`：网格

```python
grid{
    xgrid{
        line{ pos = 0.0  spacing = 0.1 }
        line{ pos = 4.6  spacing = 0.1 }
        # 每个材料界面放一个 line
    }
}
```

QCL 教程的默认网格间距 **0.1 nm**（GaAs 单胞 0.565 nm）。见 [[08-边界条件-网格-数值技巧]]。

### 2.4 加电场的两种方式（等价）

**方式 A：恒定电场（推荐用于能级设计）**

```python
poisson{
    electric_field{
        strength            = -48.5e5      # [V/m] = -48.5 kV/cm
        reference_potential = 2.1315       # 网格最左端电势 [V]，用于对齐
    }
    output_potential{ }
    output_electric_field{ }
}
```

**方式 B：两端接触偏压（有接触几何时用）**

```python
contacts{
    charge_neutral{ name = "leftgate"  bias = 0.0 }
    charge_neutral{ name = "rightgate" bias = 1.36081 }   # 1.36081 V / 152.9 nm ≈ -89 kV/cm
}
```

注意符号约定：电场方向与坐标方向相反时电场为负。偏压法算出的电场 = 电压差 / 两接触间距离。

### 2.5 `quantum`：薛定谔方程

```python
quantum{
    region{
        name = "quantum_region"
        x = [-26.1, 49.6]                  # 量子区覆盖整个器件
        no_density = yes                   # 不把量子态密度反哺泊松（非自洽）
        boundary{ x = dirichlet }          # QCL 必须 Dirichlet，勿用周期边界
        Gamma{ num_ev = 20 }               # 解 20 个本征态

        output_wavefunctions{ max_num = 9999 all_k_points = yes
                              amplitudes = no probabilities = yes }
        momentum_matrix_elements{
            polarization{ name = "component_x" re = [1, 0, 0] }
            Gamma{ }
        }
        transition_energies{ Gamma{ } }
    }
}
```

### 2.6 `run`：算哪些方程

```python
run{
    quantum{ }        # 只解薛定谔（QCL 第一步）
    # poisson{ }     # 需要泊松（自洽/掺杂）时加上
}
```

### 2.7 `database`：覆盖材料参数（校准带阶）

Page2001 示例为了让 Al₀.₄₅Ga₀.₅₅As 的导带带阶 = 390 meV（论文值），修改 bowing：

```python
database{
    bowing_zb{
        name = "AlGaAs_Bowing_Ga"
        valence = III_V
        valence_bands{ bandoffset = -0.091925 }
    }
    bowing_zb{
        name = "AlGaAs_Bowing_Al"
        valence = III_V
        valence_bands{ bandoffset = -0.091925 }
    }
}
```

## 3. 主要输出文件（bias_00000 目录）

| 路径 | 内容 |
|---|---|
| `bias_00000/Quantum/bandedges.dat` | 导带/价带边沿（画能带图用） |
| `bias_00000/Quantum/wf_energy_spectrum_quantum_region_Gamma_0000.dat` | 本征能量谱 [eV] |
| `bias_00000/Quantum/wf_probabilities_shift_quantum_region_Gamma_0000.dat` | 本征能量（每网格点一列） |
| `bias_00000/Quantum/momentum_matrix_elements_quantum_region_Gamma_100.txt` | 动量矩阵元（所有跃迁） |
| `bias_00000/Quantum/transition_energies_quantum_region_Gamma_Gamma.txt` | 跃迁能量 |
| `bias_00000/Quantum/overlap_integrals_qr1_Gamma_100.txt` | 重叠积分 |
| `bias_00000/Quantum/dipole_moment_matrix_elements_qr1_Gamma_100.txt` | 偶极矩矩阵元 |
| `bias_00000/Structure/...` | 结构信息 |

## 4. 从零写一个 QCL 输入文件的最小骨架

直接用仓库里的模板：`templates/QCL输入文件模板.nnp`

改 5 个地方就能跑：

1. 材料（阱/垒名字、合金组分）
2. 层序坐标（按 [[04-示例1-Page2001-9um-GaAs-QCL]] 的写法）
3. 电场强度与参考电势
4. 温度
5. 量子区范围与 `num_ev`

## 5. 常见坑

- 单位：电场 `V/m`（1 kV/cm = 1e5 V/m）；坐标 `nm`；掺杂 `cm⁻³`
- 层坐标不连续/重叠 → 报错或材料错乱
- 忘记 `boundary{ x = dirichlet }` → 边界波函数异常
- `num_ev` 太少 → 找不到想要的能级（每周期约 3~4 个相关态，3 周期至少解 15~20 个）
- 加了电场还开周期边界 → 物理上不自洽（见 [[08-边界条件-网格-数值技巧]]）
